export default ({ app }) => {
  //   app.router.beforeEach((to, from, next) => {
  //     const publicPages = ['/auth', '/', '/search']
  //     console.log(to.path)
  //     const authRequired = publicPages.includes(to.path)
  //     const loggedIn = localStorage.getItem('ff_user')
  //     if (authRequired && !loggedIn)
  //       return next('/auth')
  //     }
  //     next()
  //   })
}
